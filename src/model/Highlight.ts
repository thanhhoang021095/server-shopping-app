import { defaultModel } from '../common/constants'

export default {
  id: defaultModel.number,
  title : defaultModel.string,
  type : defaultModel.string,
  image : defaultModel.string,
  linkHref : defaultModel.string
}
