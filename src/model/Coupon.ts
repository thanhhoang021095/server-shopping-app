import { defaultModel } from '../common/constants'

export default {
    id: defaultModel.number,
    condition: defaultModel.number,
    code: defaultModel.string,
    value: defaultModel.number,
    description:  defaultModel.string,
}
